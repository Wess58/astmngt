import { FormatNavTitlePipe } from './format-nav-title.pipe';

describe('FormatNavTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNavTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
