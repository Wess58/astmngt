import { HasOperationDirective } from './has-operation.directive';

describe('HasOperationDirective', () => {
  it('should create an instance', () => {
    const directive = new HasOperationDirective();
    expect(directive).toBeTruthy();
  });
});
