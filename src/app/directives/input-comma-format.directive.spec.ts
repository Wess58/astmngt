import { InputCommaFormatDirective } from './input-comma-format.directive';

describe('InputCommaFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCommaFormatDirective();
    expect(directive).toBeTruthy();
  });
});
